
---
- hosts: all
  become: true
  pre_tasks:
    - name: 'install python3.6'
      raw: sudo apt-get -y install python3.6

  tasks:
    - name: navigate to backend
      shell: "mkdir /home/vishwa/Hello_from_gitactions"
